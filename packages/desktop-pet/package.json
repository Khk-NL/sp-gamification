{
  "name": "@sppet/desktop-pet",
  "version": "0.14.0",
  "private": true,
  "description": "SPPet 透明桌宠悬浮窗与本地桥接",
  "author": "SCSldr",
  "main": "src/main.cjs",
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "build": "node --check src/main.cjs && node --check src/preload.cjs && node --check src/renderer.js",
    "test": "node --test test/*.test.cjs",
    "package:win": "pnpm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "archiver": "^7.0.1",
    "electron": "^38.1.0"
  }
}
