{
  "name": "@sppet/sp-plugin",
  "version": "0.14.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "pnpm run build && tsc -p tsconfig.json --noEmit && node test/plugin-smoke.mjs",
    "package": "pnpm run build && node scripts/package.mjs"
  },
  "dependencies": {
    "@sppet/core": "workspace:*"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.9"
  }
}
