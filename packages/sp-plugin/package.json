{
  "name": "@sp-gamification/sp-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "pnpm run build && tsc -p tsconfig.json --noEmit && node test/plugin-smoke.mjs",
    "package": "pnpm run build && node scripts/package.mjs"
  },
  "dependencies": {
    "@sp-gamification/core": "workspace:*"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.9"
  }
}
