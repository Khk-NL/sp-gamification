# SPPet 开发阶段记录

本文按 `D:\Code\sppet\FEATURE_ROADMAP.md` 记录每个阶段的实际成果、验证和遗留项。未写明完成的功能均不得视为已实现。

## Phase 0：现有代码审计（2026-09-12）

### 已实现

- pnpm monorepo，现有模块为 `core`、`sp-plugin`、`desktop-pet`、`website`。
- core 已有 XP、等级、金币、streak、状态、签到、每日任务/专注委托、存档迁移、商店、背包、装备、技能、分叉地图、回合制战斗和战前/战后剧情。
- SP 插件已接入 `taskComplete`、`taskUpdate`、`taskCreated`、`currentTaskChange`、`finishDay`、同步数据变更，并使用 SP 官方持久化与 HTTP 权限。
- Electron 桌宠已有透明无边框窗口、置顶、拖动、鼠标穿透、托盘、右键设置、皮肤导入和本地 WebSocket 桥接。
- 网站已有本地 SQLite 登录、排行榜、状态工具、内容/剧情编辑器和 Nginx 配置。

### 部分实现

- 战斗、背包、食物、商店、装备、技能已可用，但还未完全拆成路线图建议的独立 package。
- 桌宠支持静态图、WebP/GIF 和特定精灵表，但尚无统一角色 manifest、AssetManager、重力/边缘站立、多显示器状态机和分层换装。
- 在线功能已有旧 `/api/*`，但尚未统一迁移到 `/api/v1/*`，签名、重放防护、每日榜/周榜和离线 Catalog 缓存仍不完整。
- 设置已有语言、通知、联网和部分玩法数值；完整隐私权限矩阵、AI、窗口识别和可选生产力模块尚未建立。

### 未实现

- AI 聊天、System Prompt、御主档案、对话历史、长期记忆。
- 专注计时器、当前窗口识别、主动提醒及明确授权流程。
- 课程表、日记、长期规划、重要日子模块接口。
- Vision、观察日记、TTS、多角色和高级扩展。
- 招募券、非付费招募与保底机制。
- 更新清单下载、SHA-256 校验和用户确认安装流程。

### 当前架构问题

- 路线图建议目录与现有可运行目录冲突。源码实际位于 `D:\Code\sppet\SPPet_Code`，成品位于 `D:\Code\sppet\SPPet_Program`；遵循“优先保持现有可运行版本”暂不整体搬迁。
- `core/engine.ts` 同时承载成长与战斗，后续应按阶段抽离，但不能一次性推倒重写。
- 插件在线 URL 与旧 API 路径仍分散；Phase 8 统一为 API Client。
- AppData 仍有多个 JSON；在保留兼容读取的前提下逐步收敛，历史/日记类数据再使用 SQLite。

### 建议复用模块

- 复用 `core` 的状态迁移、战斗和 Catalog 归一化。
- 复用插件的串行操作队列、SP hooks、单文件 iframe 构建和 WebSocket 待确认事件队列。
- 复用桌宠现有鼠标穿透、拖动、托盘和 IPC 安全边界。
- 复用网站的 SQLite 账户、静态服务、内容编辑器和 Nginx 模板。

## Phase 1：稳定核心（2026-09-12，v0.4.0）

### 已完成

- 新增通用异步 Event Bus；SP 学习事件、每日签到/复盘、连接与桌宠抚摸从统一入口分发。
- 保留 XP、金币、状态、持久化、桌宠基础交互与 SP 事件联动，存档升级到 v4 并兼容旧数据。
- 新增五阶段、500 点封顶的好感度；抚摸每日最多奖励三次，防止无限点击刷取。
- 每日委托补全任务、专注、高优先级任务和复盘四类；每日 XP 默认封顶 100。
- 每 25 分钟专注奖励 2 金币与 2 状态，每日最多四档；委托恢复状态并增加少量金币与好感。
- 新增陪伴/冒险模式；陪伴模式隐藏战斗入口且核心拒绝开战。

### 验证

- core TypeScript 编译通过。
- core 16 项测试通过，覆盖事件总线、旧奖励路径、去重、每日 XP 上限、好感度上限逻辑和陪伴模式。
- SP 插件 TypeScript 检查通过；桌宠主进程、preload 与 renderer 语法检查通过。

### 下一阶段

- Phase 2：逐项审计并完善战斗规则、意图、状态收益倍率、章节/Boss 与战斗事件边界。

## Phase 2：完善当前战斗系统（2026-09-12，v0.5.0）

### 已完成

- 复核并保留分叉地图、普通层战斗/补给二选一、章节末 Boss、战前/战后逐句剧情与跳过。
- 保留最多四技能、每回合固定恢复至 5 资源、连续出牌后手动结束回合、灼烧结算和敌方行动意图。
- 保留火克冰、冰克电、电克水、水克火 `×1.5`，同属性元素护盾 `×0.5`，其他及物理 `×1`；resistance 字段继续不参与计算。
- 明确状态收益：80–100 `×1.2`、50–79 `×1`、20–49 `×0.9`、0–19 `×0.8`，同时作用于战斗 XP 和金币。
- 战斗相关 UI 命令迁入 Event Bus；界面展示完整状态收益档位。

### 验证

- 新增状态收益边界测试，连同 Phase 1 与既有战斗回归测试集中执行。

### 下一阶段

- Phase 3：完善背包分类、可持续金币消耗、补给节点、技能强化与装备升级。

## Phase 3：背包、食物、商店、装备、技能（2026-09-12，v0.6.0）

### 已完成

- 存档 v5 新增技能等级、装备强化等级和商店轮换状态，旧存档自动迁移。
- 独立背包展示已拥有物品，可使用食物、药品和消耗品，可装备皮肤/武器/护甲/饰品。
- 技能与装备各可强化至 3 级；技能倍率仍限定为 `0.5/1/1.5`，其余强化只增加整数护盾、治疗、灼烧或固定属性。
- 商店消耗 10 金币刷新 8 件商品，食物、药品、消耗品允许重复购买，形成持续金币消费。
- 新增修复喷剂（HP +40）和奖励信标（下一场金币 +20%）。
- 强化、刷新命令接入 Event Bus，桌宠增加强化反馈。

### 验证

- 新增技能强化伤害、装备强化属性、商店刷新、药品购买/使用测试。

### 已知边界

- 补给节点当前仍为自动金币/HP 补给；状态恢复、技能强化、临时 Buff 和清除 Debuff 的可选补给将在地图内容化时继续扩展。

### 下一阶段

- Phase 4：桌宠行为状态机、重力、底部站立、屏幕边缘吸附、多显示器和统一资源 manifest/AssetManager。

## Phase 4：桌宠行为与 Ark-Pets 类交互（2026-09-12，v0.7.0）

### 已完成

- 保留 Electron 透明悬浮窗、置顶、鼠标穿透、右键菜单和系统托盘，不复制 Ark-Pets GPL 源码，仅采用动作状态机、物理与资源管理的设计思路。
- 新增重力下落、桌面工作区底部站立、左右屏幕边缘吸附和按鼠标所在显示器约束的跨屏拖动。
- 自动行走由用户明确开启，默认关闭；拖动由主进程真实光标坐标计算，相同坐标不更新窗口，避免静止漂移。
- 新增角色 `manifest.json` 与 AssetManager。支持 emoji、PNG/WebP、GIF/APNG、多帧序列和 MP3/Ogg/WAV 状态音效，资源限制在角色目录且单文件不超过 15MB。
- 预留 idle、walk、sleep、happy、sad、angry、tired、eat、touch、drag、fall、edge、battle、victory、defeat、talk；缺失状态和损坏的可选动画回退到 idle。
- 自定义角色可导入文件夹、预览动作、切换和删除，复制保存到 `%APPDATA%\SPPet\characters`；文件监听会清除缓存并热重载。
- manifest 与运行时结构预留 outfit、accessory、effect、sound 图层，首版允许切换完整资源组；美术目录拆分为角色、战斗、UI、天气和共享资源。

### 验证

- AssetManager 3 项测试覆盖缺失状态回退、角色导入/切换/删除、逐帧动画与越界资源拒绝。
- 桌宠主进程、preload、renderer 语法检查通过；后续随阶段发布执行完整构建与便携包生成。

### 已知边界

- 当前落点为 Windows 工作区底部和显示器左右边缘，不识别其他应用窗口顶部。
- Spine、Live2D、`.sppetpack`、多图层实时合成与天气渲染保留接口，按路线图后续阶段实现。

### 下一阶段

- Phase 5：独立 AI 模块、可编辑 System Prompt、御主档案、角色上下文和本地对话历史。

## Phase 5：AI 聊天与角色上下文（2026-09-12，v0.8.0）

### 已完成

- 新增独立 `AiService`，桌宠主进程仅通过服务接口读取配置、档案、历史和发起对话。
- AI 默认关闭；支持 OpenAI-compatible Chat Completions，远程接口强制 HTTPS，本机服务允许 localhost HTTP。
- 设置页可编辑性格、说话方式、世界观和关系，并显示当前完整 System Prompt；Prompt 更新后清空内存中的短期上下文。
- 新增 `%APPDATA%\SPPet\profile.json`，保存 nickname、birthday、petName、userCallPet、petCallUser、relationship、customFields，并在每次 AI 请求中注入。
- 右键菜单新增 AI 聊天和历史记录；`chat-history.json` 最多保留最近 500 轮，可按真实 id 删除或清空实际文件。
- API Key 只从 `SPPET_AI_API_KEY` 或当前设置会话进入进程内存，不写入 JSON、日志或仓库。

### 验证

- AI Service 测试覆盖档案/Prompt 持久化、API Key 不落盘、短期上下文连续、Prompt 变更重置、历史删除/清空和非本机 HTTP 拒绝。
- 桌宠主进程、preload 和 renderer 语法检查纳入阶段集中验证。

### 已知边界

- 长期记忆、embedding、本地向量检索属于路线图 Phase 9；当前删除接口已直接作用于对话历史，后续长期记忆会复用同一来源 id。
- 当前只实现文字聊天，不自动读取屏幕、窗口、任务、日记或其他私人数据。

### 下一阶段

- Phase 6：默认关闭的当前窗口/进程感知，以及明确授权和本地事件边界。

## Phase 6A：屏幕和系统感知（2026-09-12，v0.9.0）

### 已完成

- 新增独立 `SystemAwareness`，当前窗口读取和 Vision 使用两个独立开关，初始均为关闭。
- Windows 当前窗口探针只在用户手动点击后运行，读取进程名、窗口标题和采集时间；数据只保留在内存中。
- 当前窗口信息不自动写入对话、历史或其他文件，也不自动发给 AI。
- “看看我的屏幕”只在用户点击且已授权时截取鼠标所在显示器一次，缩放至最多 1280×720、JPEG 质量 60，再交给已配置的 Vision 模型。
- 截图数据只存在于单次请求内，不保存到 `chat-history.json`；历史仅记录文字提示和模型回答。
- 遵循“窗口标题 → 本地规则 → OCR → Vision”顺序预留后续能力；本阶段不启用持续监听、OCR 或自动上传。

### 验证

- 系统感知测试覆盖默认关闭、未授权不调用探针、授权后单次读取、关闭后清除内存结果和 Vision 独立授权。
- AI 测试验证 Vision 请求结构有效且实际历史文件不含截图 data URL。

### 下一阶段

- Phase 6B：专注计时器、用户自定义工作/娱乐/游戏规则、偏离提醒和每日封顶奖励。

## Phase 6B：专注系统（2026-09-12，v0.10.0）

### 已完成

- 新增独立 `FocusTimer`，提供 5–120 分钟计时、取消、重启恢复和完成状态持久化。
- 用户自行填写工作、娱乐、游戏进程/标题关键词；分类器初始没有任何预设，不硬编码价值判断。
- 只有专注进行中且用户已授权当前窗口读取时，每 10 秒进行一次本地分类；娱乐/游戏分类变化时桌宠提醒，不强制阻断应用。
- core 存档升级到 v6，新增 `processedFocusSessionIds` 与当日计时奖励次数；同一 session 永不重复结算。
- 每次有效完成提供专属 10 XP、2 金币、5 状态，并复用原有专注分钟累计与每 25 分钟奖励；专属奖励每日最多 4 次，XP 继续受每日总上限约束。
- 断连时桌宠保留 `pendingReward`，重连后重发；SP 持久化完成后返回 session ACK，桌宠收到后才允许覆盖该轮记录。

### 验证

- core 测试覆盖计时奖励、重复 session、每日 4 次封顶和分钟累计。
- FocusTimer 测试覆盖仅完成一次、待确认恢复、ACK 清除以及完全由用户规则决定的窗口分类。

### 下一阶段

- Phase 7：课程表、日记、长期规划和重要日子模块接口。

## Phase 7：可选生产力扩展（2026-09-12，v0.11.0）

### 已完成

- 新增本地 `ProductivityHub`，课程表、日记、长期目标和重要日子均拥有独立 enabled 开关，默认关闭。
- 课程记录开始/结束时间和提前提醒分钟数，按提醒键去重发送 `COURSE_STARTING`。
- 日记按日期保存文本；AI 读取使用独立权限，只有用户主动点击时读取当天一条日记并调用现有 AI Service。
- 长期目标记录目标、阶段、0–100% 进度和截止日期，进度变化发送 `GOAL_PROGRESS_UPDATED`。
- 重要日子支持 exam、birthday、deadline、anniversary，在 30/7/3/1/0 天节点发送 `IMPORTANT_DATE_APPROACHING`。
- 所有数据存于 `%APPDATA%\SPPet\productivity.json`，提醒去重独立保存；这些模块不依赖 SP、AI 或在线服务才能让桌宠运行。

### 验证

- 测试覆盖默认关闭、字段归一化、目标事件、课程/日期提醒去重，以及日记 AI 双重授权。

### 已知边界

- MVP 使用 JSON 数组编辑器，优先保证字段完整和本地可检验；表格化编辑器留到后续 UI 优化。

### 下一阶段

- Phase 8：统一 `/api/v1` API Client、服务器 Catalog 缓存、排行榜和在线内容接口。

## Phase 8：统一在线服务（2026-09-12，v0.12.0）

### 已完成

- 新增统一 `ApiClient`，所有线上地址集中在一个模块，覆盖 status、config、profile、排行榜、Catalog、release 和 events。
- 排行榜默认关闭；用户主动开启后注册随机设备 Token，服务端只保存摘要，并拒绝越界数值、异常累计增长、倒退累计值和重复 nonce。
- 排行榜提供 daily、weekly、all 三种视图，只接收公开昵称、等级、XP、streak、专注分钟、委托分和战斗分，不上传任务、日记、对话或屏幕内容。
- 在线商店、物品、装备、技能、剧情和活动采用独立 Catalog；玩家存档不进入 Catalog，插件只在请求成功后替换本地缓存。
- 更新清单提供 version、downloadUrl、changelog、sha256 和 minCompatibleVersion；插件展示日志、征求下载确认并在本地校验 ZIP，失败时保持当前版本运行。
- 依据 SP 官方限制，ZIP 安装仍由用户在“设置 → 插件 → 选择插件文件”完成；没有申请可执行任意本机代码的 `nodeExecution` 权限。

### 验证

- 网站测试覆盖设备注册、Bearer Token、排行榜提交、防重放、日榜、隐私字段、Catalog、release 和 events。
- 插件构建、TypeScript 检查和 SP iframe smoke test 通过；内嵌 CSS/JS、三项底栏和核心任务/专注/战斗路径保持有效。

### 下一阶段

- Phase 9：长期记忆、观察日记、TTS Provider，并复核现有主动 Vision 的阶段完整性。

## Phase 9：长期记忆、观察日记与 TTS（2026-09-12，v0.13.0）

### 已完成

- 保留 Phase 6A 的主动 Vision：截图权限独立且默认关闭，仅点击后截取一次压缩图，不保存图像。
- 新增本地 `MemoryStore`，将对话摘要、正文和 64 维确定性 embedding 写入 SQLite；启用后新对话检索最相关的 3 条记忆注入 Context。
- 长期记忆默认关闭，提供查看、逐条删除、关闭和清空；删除历史会通过 sourceHistoryId 删除真实关联记录，更新 Prompt 不删除长期记忆。
- 新增 `ObservationJournal`，按 id 去重保存最近 30 天的专注、战斗、喂食、心情、任务和互动事件。
- “生成今日观察日记”只有用户点击才会把当天结构化摘要交给现有 AI，并要求角色用第一人称输出；不会自动上传完整事件库。
- 新增 `TTSService` 与 disabled/local/remote Provider，使用 OpenAI 风格 audio/speech 请求；远程 HTTPS、本机 localhost HTTP，API Key 仅在会话内存。
- 播放语音时通过 Web Audio 振幅切换开/闭嘴两态，不实现复杂 Lip Sync；实时 STT 留到最后阶段。

### 验证

- 桌宠 18 项测试全部通过，覆盖记忆检索/关联删除/清空、六类观察事件/去重、TTS 默认关闭/本机调用/API Key 不落盘，以及此前全部桌宠模块。
- 主进程、preload 和 renderer 语法检查通过；插件构建、类型检查和 SP iframe smoke test 通过。

### 下一阶段

- Phase 10：多角色独立状态、昼夜/情绪/主动行为，以及高级资源扩展的最小收口。

## Phase 10：多角色与高级扩展（2026-09-12，v0.14.0）

### 已完成

- `CharacterRoster` 为每个角色保存独立状态、好感阶段、动态情绪和最近互动；AI Service 按角色使用独立数据目录，情绪进入 Prompt，但不产生严重惩罚。
- `BehaviorService` 实现默认 23:00–07:00 作息、当日唤醒、关闭/修改作息，以及默认关闭且不抢焦点的主动气泡、整点报时、系统负载和随机事件。
- AssetManager 完成基础角色、服装、挂件、特效、天气分层与安全 `.sppetpack` 导入；路径越界、符号链接、不支持压缩和超限文件会被拒绝。
- 招募券只由五日周委托、每七日签到、Boss、长期目标发放并按来源去重；十抽保底，奖池没有武器/护甲/饰品等纯数值装备，也没有付费入口。
- core 存档升级到 v7，旧状态自动补全招募数据；长期目标事件由桌宠经 Event Bus 传给插件并结算招募券。
- 战斗元素颜色、命中特效和护盾特效由 `battleVisuals` 内容配置控制；服务器内容缺项或不安全 token 会回退内置配置。
- 窗口、Vision、剪贴板、文件整理和匿名遥测拥有独立默认关闭授权；后 3 项当前版本不读取、不操作、不采集。

### 验证

- core 构建及 23 项测试通过，覆盖招募来源去重、保底、奖池约束、资源化战斗特效和既有成长/战斗路径。
- 桌宠语法检查及 21 项测试通过，覆盖多角色、昼夜行为、`.sppetpack`、分层资源和独立隐私授权。
- 插件构建、TypeScript 检查和 iframe smoke test 通过；网站测试纳入最终发布验证。

### 路线图结论

- Phase 0–10 的明确阶段目标均已有可运行实现；路线图标为“未来”“后续”“逐步”的 Spine/Live2D、实时 STT、多角色同屏交流、OCR、天气联网和可选文件整理仍作为扩展点，不冒充已完成能力。
