# 版本日志

## 0.11.0 — 可选个人计划模块（2026-09-12）

- 新增默认关闭且彼此独立的课程表、日记、长期目标和重要日子模块，本地保存于 `productivity.json`。
- 课程可配置开始/结束时间和提前提醒分钟数，触发 `COURSE_STARTING`；重要日子支持考试、生日、Deadline、纪念日和明确倒计时。
- 长期目标保存目标、阶段、0–100% 进度和截止日期，变化时发送 `GOAL_PROGRESS_UPDATED`。
- 日记的 AI 读取权限单独默认关闭；只有用户点击“让桌宠看看今天的日记”后才读取当日文本，不自动上传完整日记库。
- 提醒去重保存在独立本地文件，不依赖 SP、AI 或在线服务，关闭任一模块不影响桌宠运行。

## 0.10.0 — 本地专注监督（2026-09-12）

- 桌宠右键菜单新增 5–120 分钟专注计时器，以及工作、娱乐、游戏三类用户自定义关键词。
- 只有已授权当前窗口读取时，计时期间才每 10 秒本地检查一次；不内置软件价值判断，不上传窗口标题。
- 检测到用户自行归类的娱乐或游戏窗口时由桌宠提醒，计时继续运行，不强制阻止应用。
- 完成专注后由 core 发放 10 XP、2 金币、5 状态，并继续累计专注委托；专属奖励每天最多 4 次且共享每日 XP 上限。
- session ID 在 core 去重；桌宠断连时保留待结算记录，重连重发并等待 SP ACK，防止重启丢奖或重复奖励。

## 0.9.0 — 明确授权的系统感知（2026-09-12）

- 新增独立 System Awareness 模块，当前窗口读取与 Vision 权限分别配置且默认关闭。
- 用户授权后可手动读取当前进程名和窗口标题；结果仅在内存和设置页显示，不自动加入 AI 上下文或持久化。
- AI 聊天页新增“看看我的屏幕”：仅在用户点击后截取鼠标所在显示器、压缩为 JPEG 并发送一次，完成后不保存截图。
- Vision 请求沿用 AI 的 HTTPS/localhost、模型和会话 API Key 边界；历史只保存提示文本与回答，不写入截图数据。

## 0.8.0 — AI 角色与本地历史（2026-09-12）

- 新增独立 AI Service，兼容标准 Chat Completions 接口；AI 默认关闭，仅允许 HTTPS 或 localhost HTTP。
- 桌宠设置可编辑性格、说话方式、世界观、关系并查看实时 System Prompt；保存后立即清空短期上下文，不删除历史。
- 新增 `%APPDATA%\SPPet\profile.json` 御主档案，包含昵称、生日、宠物名、双方称呼、关系和自定义字段，并注入角色上下文。
- 右键菜单新增 AI 聊天与历史记录；历史展示时间和原文，可逐条删除或清空，操作直接修改实际本地存储。
- API Key 只在本次桌宠进程内存中保存，也支持 `SPPET_AI_API_KEY`，不会写入明文 JSON。

## 0.7.0 — 桌宠行为与角色资源（2026-09-12）

- 新增独立 `AssetManager`，集中负责角色 manifest 校验、资源路径解析、缓存、缺失状态回退、自定义角色导入/删除及热重载。
- 内置角色和自定义角色统一支持 emoji、静态 PNG/WebP、GIF/APNG、逐帧动画以及 MP3/Ogg/WAV 状态音效；16 种动作状态均已预留并可在设置中预览。
- 自定义角色复制到 `%APPDATA%\SPPet\characters`，与程序升级分离；保留旧 `pet.json` 精灵表兼容入口。
- 增加可选重力、自由落体、桌面底部站立、屏幕边缘吸附、多显示器拖拽和可选间歇行走；自动行走默认关闭，静止鼠标不会推动窗口。
- 战斗、胜利、失败、进食、抚摸、疲惫等核心事件会切换对应角色动作；角色 manifest 已预留 outfit、accessory、effect 和 sound 图层接口。

## 0.6.0 — 背包与强化循环（2026-09-12）

- 存档升级至 v5，新增技能等级、装备强化等级和每日商店轮换，旧存档自动补全。
- 增加独立背包视图，以及食物、药品、消耗品的分类购买与使用；新增修复喷剂和金币增益信标。
- 技能和装备均可强化至 3 级：技能只沿用 `0.5/1/1.5` 倍率档或增加整数效果，装备按槽位获得固定整数成长。
- 商店可消耗 10 金币刷新 8 件轮换商品；重复消耗品、强化和刷新共同构成长效金币出口。
- 强化和商店刷新通过 Event Bus 进入 core，桌宠会反馈技能与装备强化结果。

## 0.5.0 — 战斗规则收口（2026-09-12）

- Phase 2 按路线图复核现有分叉地图、Boss、四技能、5 点资源、逐句剧情、敌方意图、元素克制、元素护盾、灼烧与简化 Buff/Debuff，保持 resistance 不参与伤害。
- 状态值正式影响战斗 XP 与金币：80–100 为 `×1.2`，50–79 为 `×1`，20–49 为 `×0.9`，0–19 为 `×0.8`；界面直接显示所有阈值和倍率。
- 开战、地图补给、剧情推进、技能使用与结束回合全部通过统一 Event Bus 进入 core，减少 SP 适配层与战斗模块的直接耦合。

## 0.4.0 — 核心稳定与统一事件（2026-09-12）

- 新增通用异步 `EventBus`，SP 的任务完成、专注累计、每日复盘、签到、桌宠连接与抚摸统一通过事件入口进入核心。
- 存档升级到 v4，旧存档自动补全且保留原进度；新增“陪伴/冒险”模式，陪伴模式会完全关闭战斗入口。
- 新增封顶的五阶段好感度：陌生、熟悉、亲近、信赖、挚友；签到、每日委托、喂食和每日前三次抚摸可增加好感。
- 每日委托扩展为任务、60 分钟专注、高优先级任务和每日复盘四类；SP `finishDay` 事件会结算每日复盘。
- 新增每日 XP 上限（默认 100），任务本身不直接产出 XP；委托、战斗共享该上限，避免重复创建任务刷经验。
- 每累计 25 分钟专注（每日最多四档）获得 2 金币和 2 状态；每项委托额外恢复 5 状态并提供少量金币。
- 桌宠左键互动会上报 `PET_TOUCHED`，悬浮信息新增好感阶段；插件概览新增状态、好感、四类委托和当日 XP 上限。

## 0.3.5 — 大字战斗与四技能修复（2026-09-11）

- 插件所有正文、说明、按钮与英文技术标签再次统一放大并加粗；桌宠气泡、设置菜单和网站编辑模块同步提高最小字号。
- 新旧存档都会解锁 4 个基础技能，首次使用默认装备 4 个；修复技能商店已学习状态判断，可在非战斗状态正确选择最多 4 个技能。
- 战术打击补充 `攻击 ×1` 数值说明；技能组合和战斗按钮会直接显示当前伤害或本次护盾前伤害。
- 玩家看到的伤害公式简化为“技能伤害 × 属性效果 − 防御；护盾先抵扣伤害”，属性倍率统一显示为 `×1`、`×1.5`、`×0.5`。
- 百分比效果收敛到 `10%`、`20%`、`50%` 三档，旧状态与网站内容加载时也会自动归档；熔核长枪和霜线套装的元素增伤统一为 `20%`，技能倍率只使用 `0.5`、`1`、`1.5`。
- 每漏登 1 天扣 10 状态；状态扣完后，剩余惩罚按 1:1 扣 HP。同日检查不会重复结算，桌宠会显示具体扣除值。

## 0.3.4 — 四技能回合与简化路线（2026-09-11）

- 玩家可从已学习技能中自行装备最多 4 个技能；每回合可连续使用多个技能，只有点击“结束回合”才结算灼烧和敌方行动。
- 战斗资源固定为每回合 5 点，不再逐回合提高上限。
- 章节地图改为由下向上的简化分叉路线：普通层只能在战斗与补给中二选一，末层强制挑战 Boss。
- 简化伤害系统：克制 `×1.50`、同属性元素护盾 `×0.50`、其他 `×1.00`；物理不参与元素交互，抗性退出伤害结算。
- 战斗界面加入宠物 HP 条、属性克制表、完整伤害公式，以及独立的 Buff / Debuff 状态模块。
- 插件、桌宠和网站的英文技术标签统一改为加粗系统字体并提高最小字号，移除难以辨认的小号像素字体。
- 桌宠拖动使用固定起点计算位移，相同鼠标坐标不再重复移动窗口；拖动时隐藏气泡并停用角色位移动画，修复静止漂移与气泡分离。

## 0.3.3 — 像素远征、属性战斗与桌宠输入修复（2026-09-11）

- 插件切换为高饱和、温馨的像素冒险风格，使用本地内嵌像素素材与加粗字体，保持 SP 单文件侧栏兼容。
- 将养成与战斗统一到“冒险”板块；战斗页先显示章节小地图，加入可领取的补给节点和按顺序解锁的战斗节点。
- 加入火克冰、冰克电、电克水、水克火的明确 `×1.50/×0.75` 关系；同属性元素护盾伤害 `×0.65`，物理伤害固定 `×1.00` 且不参与元素交互。
- 五类攻击和五类护盾分别使用不同颜色、轮廓与命中特效；战斗日志显示倍率、护盾吸收值和最终伤害，不使用模糊数量描述。
- 战斗开始、胜利和战败新增文字提示与独立特效，桌宠同步响应这些事件。
- 桌宠透明区域默认鼠标穿透，仅在角色、菜单和设置区域恢复交互；拖动改为主进程读取真实鼠标位置，修复全屏点击被拦截和静止漂移。
- 网站保留既有战术档案风格，同时加粗中英文字体并放大导航、面板、排行榜和编辑器控件。

## 0.3.2 — SP 内嵌界面与桌宠拖动（2026-09-11）

- 根据 SP 上传插件的实际加载机制，将 CSS 和 UI JavaScript 自动内嵌到发布版 `index.html`，修复侧边栏只有纯文本、按钮无响应的问题。
- 构建时校验单文件界面未引用 ZIP 内的相对资源，并保持在 SP 的 100 KB 限制内。
- 桌宠角色本体支持按住左键拖动；小幅移动阈值避免误触，单击台词行为保持不变。
- Codex Pet v1/v2 精灵表在拖动时分别使用向左/向右奔跑动画行，松开后恢复待机动画。

## 0.3.1 — 侧边栏修复与本地账号（2026-09-11）

- 修复插件 iframe 直接调用 `PluginAPI` 导致的脚本初始化中断。
- 使用 SP 插件已验证的 `./index.css`、`./index.js` 相对资源形式，并增加打包回归检查。
- 插件、桌宠和网站改为深浅斜向分区、档案编号、网格/准星与蓝色信号线视觉。
- 开发者网站增加完整响应式 CSS，并修复本地资源相对路径。
- 新增 `start-local.bat`，双击即可启动网站并打开登录页。
- 新增 SQLite 用户、scrypt 密码、HttpOnly 会话、注册/登录/退出接口；管理员登录后可发布剧情与数值。

## 0.3.0 — SPPet 战术养成版（2026-09-11）

### 品牌与结构

- 产品统一更名为 SPPet，包名改为 `@sppet/*`，发布物改用 `SPPet-*`。
- `core` 拆分为类型、内容目录和游戏引擎，插件界面拆分为 HTML/CSS/JS。
- 保留旧插件 ID 与同步状态键，只用于 v0.1/v0.2 数据升级兼容。

### 养成与经济

- 宠物新增 HP、攻击、防御、物理/火/水/冰/电抗性和状态值。
- 升级会提高最大生命、攻击和防御，并发放金币。
- 金币来源改为签到、升级和战斗；经验来源改为委托和战斗。
- 增加连续签到递增奖励、断连状态衰减、食物恢复和一次性战斗 Buff。
- 商店新增皮肤部件、技能芯片、武器、护甲、饰品和食物。
- 增加装备属性、元素伤害加成以及先遣/霜线套装效果。

### 冒险战斗

- 怪物只在“冒险”页面加载；主导航固定为概览、养成、冒险、设置。
- 新增回合制战斗：每回合资源点回满且上限 +1，战斗结束重置。
- 新增物理、火、水、冰、电五类伤害，抗性、护盾、灼烧、削弱和治疗。
- 敌人按章节排列，每章包含普通敌人与小 Boss，并显示下一步行动意图。
- 每场战斗加入可逐句播放、可跳过、可恢复的战前和战后剧情。

### 桌宠

- 默认窗口只显示透明桌宠，不再常驻标题栏、统计面板和卡片背景。
- 悬停显示等级、XP、状态、streak 和金币。
- 左键与间歇性闲置台词可在插件“养成 → 宠物设置”中配置。
- 右键菜单进入桌宠设置，集中管理连接、皮肤、大小和始终置顶。
- 数据目录迁移到 `%APPDATA%\SPPet`，首次启动会复制旧目录中的兼容数据。

### 网站

- 排行榜从插件导航移除，改为设置页中的可选同步与外部链接。
- 新增浏览器本地状态导入/导出工具。
- 新增战前/战后剧情编辑器和道具、技能、怪物、抗性、意图、奖励数值终端。
- 内容发布接口要求服务器环境变量 `SPPET_ADMIN_KEY`。

## 0.2.0 — 像素冒险版

- 增加 Boss、每日委托、饰品商店、中英文设置、排行榜客户端和 WebSocket 桌宠桥接。
- 增加 Codex Pet `pet.json + spritesheet` 自定义皮肤兼容。

## 0.1.0 — 初始 MVP

- 增加任务 XP/金币、等级、streak、专注奖励、SP 侧边面板与 Electron 透明桌宠。
